specVersion: v0.1.0
package:
  name: svm_clickhouse_nfts
  version: v1.0.0
  url: https://github.com/pinax-network/substreams-svm
  description: NFT Marketplaces for Solana (ClickHouse).
  image: ../../image.png

imports:
  db: ../../spkg/svm-nfts-v0.3.0.spkg

modules:
  - name: db_out
    use: db:db_out

sink:
  module: db_out
  type: sf.substreams.sink.sql.v1.Service
  config:
    schema: "./schema.sql"
    engine: clickhouse
    postgraphile_frontend:
      enabled: false

network: solana

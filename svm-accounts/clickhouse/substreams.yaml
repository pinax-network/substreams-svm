specVersion: v0.1.0
package:
  name: svm_clickhouse_accounts
  version: v0.3.1
  url: https://github.com/pinax-network/substreams-svm
  description: SPL & SPL-2022 token account events for Solana blockchains (ClickHouse).
  image: ../../image.png

imports:
  db: ../../spkg/svm-accounts-v0.3.0.spkg

modules:
  - name: db_out
    use: db:db_out

sink:
  module: db_out
  type: sf.substreams.sink.sql.v1.Service
  config:
    schema: "./schema.sql"
    engine: clickhouse
    postgraphile_frontend:
      enabled: false

network: solana

specVersion: v0.1.0
package:
  name: sanctum
  version: v0.4.0
  url: https://github.com/pinax-network/substreams-svm
  description: Sanctum swap events for Solana.
  image: ../../image.png

imports:
  solana_common: ../../../spkg/solana-common-v0.4.0.spkg

binaries:
  default:
    type: wasm/rust-v1+wasm-bindgen-shims
    file: ../../target/wasm32-unknown-unknown/release/sanctum.wasm

protobuf:
  files:
    - v1/staking/sanctum.proto
  importPaths:
    - ../../proto

modules:
  - name: map_events
    kind: map
    inputs:
      - map: solana_common:blocks_without_votes
    blockFilter:
      module: solana_common:program_ids_without_votes
      query:
        string: "program:5ocnV1qiCgaQR8Jb8xWnVbApfaygJ8tNoZfgPwsgx9kx"
    output:
      type: proto:sanctum.v1.Events

network: solana

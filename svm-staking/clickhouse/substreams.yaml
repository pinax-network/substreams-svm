specVersion: v0.1.0
package:
  name: svm_clickhouse_staking
  version: v0.3.1
  url: https://github.com/pinax-network/substreams-svm
  description: Native & Liquid staking protocols for Solana (ClickHouse).
  image: ../../image.png

imports:
  db: ../../spkg/svm-staking-v0.3.0.spkg

modules:
  - name: db_out
    use: db:db_out

sink:
  module: db_out
  type: sf.substreams.sink.sql.v1.Service
  config:
    schema: "./schema.sql"
    engine: clickhouse
    postgraphile_frontend:
      enabled: false

network: solana

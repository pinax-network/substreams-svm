specVersion: v0.1.0
package:
  name: svm_clickhouse_metadata
  version: v0.3.2
  url: https://github.com/pinax-network/substreams-svm
  description: SPL Token & Metaplex metadata events for Solana blockchains (ClickHouse).
  image: ../../image.png

imports:
  db: ../../spkg/svm-metadata-v0.3.0.spkg

modules:
  - name: db_out
    use: db:db_out

sink:
  module: db_out
  type: sf.substreams.sink.sql.v1.Service
  config:
    schema: "./schema.sql"
    engine: clickhouse
    postgraphile_frontend:
      enabled: false

network: solana
